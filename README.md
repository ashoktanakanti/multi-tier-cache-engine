# Multi-Tier Caching System (DSA + Redis + SQL)

A movie-catalog project combining a normalized relational database, a
hand-built LRU cache, and Redis as a caching layer.

Work in progress — architecture and usage docs coming as the project develops.
